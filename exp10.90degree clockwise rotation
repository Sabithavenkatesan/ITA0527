import cv2
img = cv2.imread("image.jpg")
img_small = cv2.resize(img, (600, 400))
rotated = cv2.rotate(img_small, cv2.ROTATE_90_CLOCKWISE)
cv2.imshow("Original Image", img_small)
cv2.imshow("Rotated Image", rotated)
cv2.waitKey(0)
cv2.destroyAllWindows()
