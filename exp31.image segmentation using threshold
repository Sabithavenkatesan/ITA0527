import cv2
img = cv2.imread('image.jpg',0)
_,th = cv2.threshold(img,127,255,cv2.THRESH_BINARY)
cv2.imshow("Original",cv2.resize(img,(300,300)))
cv2.imshow("Segmented",cv2.resize(th,(300,300)))
cv2.waitKey(0)
cv2.destroyAllWindows()
