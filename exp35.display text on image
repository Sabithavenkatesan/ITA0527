import cv2
import numpy as np
img = np.ones((400,400,3),dtype="uint8")*255
text = input("Enter text: ")
cv2.putText(img,text,(50,200),cv2.FONT_HERSHEY_SIMPLEX,1,(0,0,255),2)
cv2.imshow("Text Image",cv2.resize(img,(300,300)))
cv2.waitKey(0)
cv2.destroyAllWindows()
